export interface Fornecedor {
  id: number;
  nome: string;
  rua: string;
  numero: number;
  bairro: string;
  cep: string;
}

/* export interface Endereco {
  rua: string;
  numero: number;
  bairro: string;
  cep: string;
}
 */
